{
  "name": "internship-monorepo",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "npm -ws run build",
    "dev": "npm -ws run dev",
    "test": "npm -ws run test"
  }
}
